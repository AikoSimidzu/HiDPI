<Window x:Class="HiDPI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HiDPI"
        xmlns:viewModel="clr-namespace:HiDPI.MVVM.ViewModel"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#01FFFFFF"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        MouseDown="Window_MouseDown">

    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>

    <Border Background="#121111" BorderThickness="1" BorderBrush="#2d2d2d" CornerRadius="13">
        <Grid RowDefinitions="25, *, 22" ColumnDefinitions="5, 50, *, 25, 25, 5">
            <Grid>
                <tb:TaskbarIcon x:Name="HiDPIIcon" TrayMouseDoubleClick="HiDPIIcon_TrayMouseDoubleClick" Visibility="Collapsed" ToolTipText="HiDPI"
                    xmlns:tb="http://www.hardcodet.net/taskbar" IconSource="/Resources/HiDPI.ico"                    
                    LeftClickCommand="{Binding ShowWindowCommand}" DoubleClickCommand="{Binding ShowWindowCommand}">
                    <tb:TaskbarIcon.ContextMenu>
                        <ContextMenu Style="{StaticResource TrayContextMenuStyle}">
                            <MenuItem x:Name="MenuOpen" Header="Развернуть" Style="{StaticResource TrayMenuItemStyle}" Click="MenuOpen_Click" FontWeight="Bold"/>      
                            <MenuItem x:Name="MenuExit" Header="Выход" Command="{Binding StopZapret}" Style="{StaticResource TrayMenuItemStyle}" Click="MenuExit_Click"/>                            
                        </ContextMenu>
                    </tb:TaskbarIcon.ContextMenu>
                </tb:TaskbarIcon>
            </Grid>

            <Label Content="HiDPI" Foreground="White" FontSize="13" FontWeight="DemiBold" HorizontalAlignment="Center" Grid.ColumnSpan="7"/>

            <Button x:Name="ToTray" Click="ToTray_Click" Content="X" FontWeight="Bold" Grid.Column="4" Width="25" Height="25"/>
            <Button x:Name="Minimize" Click="Minimize_Click" Content="-" FontWeight="Bold" Grid.Column="3" Width="25" Height="25"/>

            <Grid Grid.Row="1" Grid.Column="1" RowDefinitions="50, 50, 50, 50">
                <Button Width="40" Height="40" Command="{Binding ConnectVC}">
                    <Image Source="/Resources/technology.png"/>
                </Button>
                <Button Width="40" Height="40" Command="{Binding LogsVC}" Grid.Row="1">
                    <Image Source="/Resources/files.png"/>
                </Button>
                <Button Width="40" Height="40" Command="{Binding DonateVC}" Grid.Row="2">
                    <Image Source="/Resources/donate.png"/>
                </Button>
                <Button Width="40" Height="40" Command="{Binding SettingsVC}" Grid.Row="3">
                    <Image Source="/Resources/setting.png"/>
                </Button>
            </Grid>

            <Border Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="3">
                <ContentControl Margin="0" Content="{Binding CurrentView}"/>
            </Border>
        </Grid>
    </Border>
    
</Window>
