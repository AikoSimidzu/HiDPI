<UserControl x:Class="HiDPI.PingForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HiDPI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#121111">
    
    <Grid ColumnDefinitions="300, 350, *">
        <Grid RowDefinitions="15, 40, 5, 40, *">
            <Button Grid.Row="1" Command="{Binding DataContext.StartPing, RelativeSource={RelativeSource AncestorType=Window }}" Content="Запустить тест" FontSize="13" Width="180"/>
            <Button Grid.Row="3" Command="{Binding DataContext.StartPingAllCfg, RelativeSource={RelativeSource AncestorType=Window }}" Content="Тест всех конфигов" FontSize="13" Width="180"/>
        </Grid>
        
        <ListBox x:Name="LogList" Grid.Column="1" Background="#121111" ItemsSource="{Binding DataContext.PingLogs, RelativeSource={RelativeSource AncestorType=Window }}" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <TextBlock Text="{Binding Name}" Foreground="White" Margin="0,0,8,0" FontSize="11"/>
                        <TextBlock Text="{Binding DisplayStatus}" x:Name="TxtBlock">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSuccess}" Value="false">
                                            <Setter Property="Foreground" Value="#FF6B6B"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        
                                        <DataTrigger Binding="{Binding IsSuccess}" Value="true">
                                            <Setter Property="Foreground" Value="#4EC9B0"/>
                                        </DataTrigger>
                                        
                                        <DataTrigger Binding="{Binding IsTest}" Value="true">
                                            <Setter Property="Foreground" Value="#F5DD27"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
